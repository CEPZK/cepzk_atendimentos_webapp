/**
 * `?from=` keys used so the assistido detail screen knows where to route
 * the "Voltar" link back to. Living in their own module (not inside the
 * page.tsx) because Next.js does not allow arbitrary named exports from
 * a page file.
 */
export const DI_I_FROM = "di-i";
export const DI_II_FROM = "di-ii";
